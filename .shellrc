
# Detect shell and apply matching settings
if [ -n "$BASH_VERSION" ]; then
  PS1="\u@\h \W $ "
  HISTSIZE=5000
  HISTFILESIZE=5000
  HISTCONTROL=ignorespace:ignoredups:erasedups
  shopt -s histappend
elif [ -n "$ZSH_VERSION" ]; then
  PROMPT="%n@%m %1~ $ "
  HISTSIZE=5000
  SAVEHIST=5000
  setopt HIST_IGNORE_SPACE HIST_IGNORE_DUPS HIST_IGNORE_ALL_DUPS APPEND_HISTORY INTERACTIVE_COMMENTS
fi


###############
### Aliases ###
###############
alias his=" history"
alias cl=" clear"

alias t="terraform"
alias k="kubectl"
alias d="docker"

alias python="python3"
alias pip="pip3"

alias lsl="ls -l"
alias lsla="ls -la"

alias dush="du -sh *"
